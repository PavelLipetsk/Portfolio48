import { FLATS_LIST } from "@/constants";
const state = {
    data: FLATS_LIST,
    
}
const mutations = {
    addFavourite(state, ) {
        state.data.FLATS_LIST.id = true;
      },

}
export default {
    state,
    // actions,
    mutations,
    // getters
  }
  console.log(FLATS_LIST);
import { createStore } from 'vuex'
import flat from '@/store/modules/flat'
export default createStore({
  state:() => ({
    
  }),
  getters: {
  },
  mutations: {
   
  },
  actions: {
  },
  modules: {
    flat
  }
})
